# Contributing
Use WordPress coding conventions, escape output, sanitize input, preserve keyboard accessibility, and test desktop, touch, mobile, RTL content, WooCommerce, and `WP_DEBUG` before submitting changes. Keep the mega-menu feature usable without a page builder.
